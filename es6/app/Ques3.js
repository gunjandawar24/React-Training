const song = {
  name: 'Dying to live',
  artist: 'Tupac',
  featuring: 'Biggie Smalls'
};


console.log(`<div class="song"
    <p>
      ${song.name} — ${song.artist}
      (Featuring ${song.featuring})
    </p>
  </div>
`);

export{song};
